--[[
  AIcrowd Evaluations API
 
  API to create and evaluate custom challenges on AIcrowd!
 
  OpenAPI spec version: 1.0.0
  
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- cluster class
local cluster = {}
local cluster_mt = {
	__name = "cluster";
	__index = cluster;
}

local function cast_cluster(t)
	return setmetatable(t, cluster_mt)
end

local function new_cluster(id, created, updated, remote_address, auth_token, docker_username, docker_registry, namespace, storage_class, status, wf_name, argo_host, argo_token, minio_user, s3_policy, user_id, organisation_id)
	return cast_cluster({
		["id"] = id;
		["created"] = created;
		["updated"] = updated;
		["remote_address"] = remote_address;
		["auth_token"] = auth_token;
		["docker_username"] = docker_username;
		["docker_registry"] = docker_registry;
		["namespace"] = namespace;
		["storage_class"] = storage_class;
		["status"] = status;
		["wf_name"] = wf_name;
		["argo_host"] = argo_host;
		["argo_token"] = argo_token;
		["minio_user"] = minio_user;
		["s3_policy"] = s3_policy;
		["user_id"] = user_id;
		["organisation_id"] = organisation_id;
	})
end

return {
	cast = cast_cluster;
	new = new_cluster;
}
