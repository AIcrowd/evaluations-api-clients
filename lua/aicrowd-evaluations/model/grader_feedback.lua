--[[
  AIcrowd Evaluations API
 
  API to create and evaluate custom challenges on AIcrowd!
 
  OpenAPI spec version: 1.0.0
  
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- grader_feedback class
local grader_feedback = {}
local grader_feedback_mt = {
	__name = "grader_feedback";
	__index = grader_feedback;
}

local function cast_grader_feedback(t)
	return setmetatable(t, grader_feedback_mt)
end

local function new_grader_feedback(dataset, status, notifications, workflow_spec, submission_types)
	return cast_grader_feedback({
		["dataset"] = dataset;
		["status"] = status;
		["notifications"] = notifications;
		["workflow_spec"] = workflow_spec;
		["submission_types"] = submission_types;
	})
end

return {
	cast = cast_grader_feedback;
	new = new_grader_feedback;
}
