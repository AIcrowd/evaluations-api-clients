--[[
  AIcrowd Evaluations API
 
  API to create and evaluate custom challenges on AIcrowd!
 
  OpenAPI spec version: 1.0.0
  
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- organisation class
local organisation = {}
local organisation_mt = {
	__name = "organisation";
	__index = organisation;
}

local function cast_organisation(t)
	return setmetatable(t, organisation_mt)
end

local function new_organisation(id, name, poc_email, created_on, total_quota, quota)
	return cast_organisation({
		["id"] = id;
		["name"] = name;
		["poc_email"] = poc_email;
		["created_on"] = created_on;
		["total_quota"] = total_quota;
		["quota"] = quota;
	})
end

return {
	cast = cast_organisation;
	new = new_organisation;
}
