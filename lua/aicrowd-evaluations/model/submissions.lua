--[[
  AIcrowd Evaluations API
 
  API to create and evaluate custom challenges on AIcrowd!
 
  OpenAPI spec version: 1.0.0
  
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- submissions class
local submissions = {}
local submissions_mt = {
	__name = "submissions";
	__index = submissions;
}

local function cast_submissions(t)
	return setmetatable(t, submissions_mt)
end

local function new_submissions(id, created, updated, grader_id, submission_data, status, output, additional_outputs, logs, started, ended, meta, wf_name, workflow_priority, minimal_run, user_id, organisation_id)
	return cast_submissions({
		["id"] = id;
		["created"] = created;
		["updated"] = updated;
		["grader_id"] = grader_id;
		["submission_data"] = submission_data;
		["status"] = status;
		["output"] = output;
		["additional_outputs"] = additional_outputs;
		["logs"] = logs;
		["started"] = started;
		["ended"] = ended;
		["meta"] = meta;
		["wf_name"] = wf_name;
		["workflow_priority"] = workflow_priority;
		["minimal_run"] = minimal_run;
		["user_id"] = user_id;
		["organisation_id"] = organisation_id;
	})
end

return {
	cast = cast_submissions;
	new = new_submissions;
}
