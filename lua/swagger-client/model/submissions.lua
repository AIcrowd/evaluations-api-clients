--[[
  Evaluations API
 
  API to create and evaluate custom challenges
 
  OpenAPI spec version: 1.0.0
  
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

-- submissions class
local submissions = {}
local submissions_mt = {
	__name = "submissions";
	__index = submissions;
}

local function cast_submissions(t)
	return setmetatable(t, submissions_mt)
end

local function new_submissions(id, created, updated, participant_id, round_id, grader_id, submission_code, status, output, additional_outputs, logs, started, ended, meta, user_id, organisation_id)
	return cast_submissions({
		["id"] = id;
		["created"] = created;
		["updated"] = updated;
		["participant_id"] = participant_id;
		["round_id"] = round_id;
		["grader_id"] = grader_id;
		["submission_code"] = submission_code;
		["status"] = status;
		["output"] = output;
		["additional_outputs"] = additional_outputs;
		["logs"] = logs;
		["started"] = started;
		["ended"] = ended;
		["meta"] = meta;
		["user_id"] = user_id;
		["organisation_id"] = organisation_id;
	})
end

return {
	cast = cast_submissions;
	new = new_submissions;
}
