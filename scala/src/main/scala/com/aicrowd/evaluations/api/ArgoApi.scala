/**
 * Evaluations API
 * API to create and evaluate custom challenges
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

package com.aicrowd.evaluations.api

import java.text.SimpleDateFormat

import com.aicrowd.evaluations.models.GenericFeedback
import com.aicrowd.evaluations.models.GraderFeedback
import com.aicrowd.evaluations.models.SubmissionFeedback
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date
import java.util.TimeZone

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

import org.json4s._

class ArgoApi(
  val defBasePath: String = "https://localhost/v1",
  defApiInvoker: ApiInvoker = ApiInvoker
) {
  private lazy val dateTimeFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSZ")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  private val dateFormatter = {
    val formatter = new SimpleDateFormat("yyyy-MM-dd")
    formatter.setTimeZone(TimeZone.getTimeZone("UTC"))
    formatter
  }
  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = dateTimeFormatter
  }
  implicit val stringReader: ClientResponseReader[String] = ClientResponseReaders.StringReader
  implicit val unitReader: ClientResponseReader[Unit] = ClientResponseReaders.UnitReader
  implicit val jvalueReader: ClientResponseReader[JValue] = ClientResponseReaders.JValueReader
  implicit val jsonReader: ClientResponseReader[Nothing] = JsonFormatsReader
  implicit val stringWriter: RequestWriter[String] = RequestWriters.StringWriter
  implicit val jsonWriter: RequestWriter[Nothing] = JsonFormatsWriter

  var basePath: String = defBasePath
  var apiInvoker: ApiInvoker = defApiInvoker

  def addHeader(key: String, value: String): mutable.HashMap[String, String] = {
    apiInvoker.defaultHeaders += key -> value
  }

  val config: SwaggerConfig = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new ArgoApiAsyncHelper(client, config)

  /**
   * 
   * Update the status of an object
   *
   * @param modelName  
   * @param objectId  
   * @param payload  
   * @return void
   */
  def postGenericFeedbackContract(modelName: String, objectId: Integer, payload: GenericFeedback) = {
    val await = Try(Await.result(postGenericFeedbackContractAsync(modelName, objectId, payload), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * Update the status of an object
   *
   * @param modelName  
   * @param objectId  
   * @param payload  
   * @return Future(void)
   */
  def postGenericFeedbackContractAsync(modelName: String, objectId: Integer, payload: GenericFeedback) = {
      helper.postGenericFeedbackContract(modelName, objectId, payload)
  }

  /**
   * 
   * Update the grader details
   *
   * @param graderId  
   * @param payload  
   * @return void
   */
  def postGraderFeedbackDao(graderId: Integer, payload: GraderFeedback) = {
    val await = Try(Await.result(postGraderFeedbackDaoAsync(graderId, payload), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * Update the grader details
   *
   * @param graderId  
   * @param payload  
   * @return Future(void)
   */
  def postGraderFeedbackDaoAsync(graderId: Integer, payload: GraderFeedback) = {
      helper.postGraderFeedbackDao(graderId, payload)
  }

  /**
   * 
   * Update the submission details
   *
   * @param submissionId  
   * @param payload  
   * @return void
   */
  def postSubmissionFeedbackDao(submissionId: Integer, payload: SubmissionFeedback) = {
    val await = Try(Await.result(postSubmissionFeedbackDaoAsync(submissionId, payload), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   *  asynchronously
   * Update the submission details
   *
   * @param submissionId  
   * @param payload  
   * @return Future(void)
   */
  def postSubmissionFeedbackDaoAsync(submissionId: Integer, payload: SubmissionFeedback) = {
      helper.postSubmissionFeedbackDao(submissionId, payload)
  }

}

class ArgoApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def postGenericFeedbackContract(modelName: String,
    objectId: Integer,
    payload: GenericFeedback)(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[GenericFeedback]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/argo/{model_name}/{object_id}")
      replaceAll("\\{" + "model_name" + "\\}", modelName.toString)
      replaceAll("\\{" + "object_id" + "\\}", objectId.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (modelName == null) throw new Exception("Missing required parameter 'modelName' when calling ArgoApi->postGenericFeedbackContract")

    if (payload == null) throw new Exception("Missing required parameter 'payload' when calling ArgoApi->postGenericFeedbackContract")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(payload))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postGraderFeedbackDao(graderId: Integer,
    payload: GraderFeedback)(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[GraderFeedback]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/argo/graders/{grader_id}")
      replaceAll("\\{" + "grader_id" + "\\}", graderId.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (payload == null) throw new Exception("Missing required parameter 'payload' when calling ArgoApi->postGraderFeedbackDao")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(payload))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def postSubmissionFeedbackDao(submissionId: Integer,
    payload: SubmissionFeedback)(implicit reader: ClientResponseReader[Unit], writer: RequestWriter[SubmissionFeedback]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/argo/submissions/{submission_id}")
      replaceAll("\\{" + "submission_id" + "\\}", submissionId.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (payload == null) throw new Exception("Missing required parameter 'payload' when calling ArgoApi->postSubmissionFeedbackDao")

    val resFuture = client.submit("POST", path, queryParams.toMap, headerParams.toMap, writer.write(payload))
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
